# Создаем словарь per_cent с распределением процентных ставок по вкладам в различных банках
per_cent = {'ТКБ': 5.6, 'СКБ': 5.9, 'ВТБ': 4.28, 'СБЕР': 4.0}

# Создаем переменную money, принимающую на вход сумму, которую планируете положить под проценты
money = int(input('Введите сумму вклада'))
TKB = int((per_cent['ТКБ']) * (money/100))
SKB = int((per_cent['СКБ']) * (money/100))
VTB = int((per_cent['ВТБ']) * (money/100))
SBER = int((per_cent['СБЕР']) * (money/100))

deposit = [TKB, SKB, VTB, SBER]
bank_deposit = {'ТКБ': 5.6, 'СКБ': 5.9, 'ВТБ': 4.28, 'СБЕР': 4.0}

print("Накопленные средства за год вклада в каждом из банков =",deposit)

print(deposit)
#print (max(bank_deposit, key=bank_deposit.get))
print ("Максимальная сумма, которую вы можете заработать — " + str(max_deposit))
